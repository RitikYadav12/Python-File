Q.1. Write a Numpy program to concatenate element-wise two array of string.
ans.1. import numpy as np
x1=NP.array(['c','programming'],dtype=np.str)
x2=NP.array(['python','programming'],dtype=np.str)
print("array 1:")
print(x1)
print("array 2:")
print(x2)

new_array=np.char.add(x1,x2)
print("new array:")
print(new_array)

Q.2. write a numpy program to repeat all the elements three times of a given array of string.
ans.2. import numpy as np
x1=NP.array(['python','programming'],dtype=np.str)
print("original array:")
print(x1)
new_array=np.char.multiply(x1,3)
print("new_array:")
print(new_array)

Q.3. write a numpy program to capitilize the first letter,lowercase,uppercase,swapcase,title-case of all the elements ofa an array.
import numpy as np
x1=NP.array(['python','programming'],dtype=np.str)
print("original array:")
print(x1)
capitilized_case=np.char.capitilized(x1)
lowered_case=np.char.lowered(x1)
uppered_case=np.char.uppered(x1)
swapcased_case=np.char.swapcased(x1)
titlecased_case=np.char.titlecased(x1)
print("\n capitilized:",capitilized_case)
print("lowered:",lowered_case)
print("uppered:"uppered_case)
print("swapcased:",swapcased_case)
print("title cased:",title cased_case)

Q.4.write a numpy program to make the length of each elements 15 of a given array and the string centred/left-justified/right-justified with paddings of_
import numpy as np 
x=np.array(['python exercise','php','c++'],dtype=np.str)
print("original array:")
print(x)
centered=np.char.centre(x,15,fillchar='_')
left=np.char.ljust(x,15,fillchar'_')
right=np.char.rjust(x,15,fillchar'_')
print("\ncentered=",centered)
print("Left=",left)
print("Right",right)

Q.5. Write a numpy program to encode all the elements of a given array in cp500 and decode it again.
import numpy as np
x=np.array(['python','java','c++''],dtype=np.str)
print("original array:")
print(x)
encoded_char=np.char.encode(x,'cp500')
decoded_char=np.char.decode(encoded_char,'cp500')
print("\nencoded=",encoded_char)
print("decoded=",decoded_char)

  Q.6. write a numpy program to remove all the leading and traillinng  whitespaces of all the elements of a given array.
  import numpy as np
  x=np.array(['python exercises','python','c++'],dtype=np.str)
  print("original array:")
  print(x)
  stripped=np.char.strip(x)
  print("\nRemove the leading and trailling whitespaces:",stripped)
  
  Q.7. write a numpy program to remove the leading whitespaces of all the given elements of an given array.
  import numpy as np
  x=np.array(['python'.'java','c++'],dtype=np.str)
  print("original array:")
  print(x)
  restripped_char=np.char.rstrip(x)
  print("\nRemove the trailling whitespaces:",rstripped_char)
  
  Q.8. write a numpy  program to split the elements of a given array with spaces
  import numpy as np
  x=np.array(['python exercises','java','python'],dtype=np.str)
  print("original array:")
  print(x)
  r=np.char.split(x)
  print("\nnsplit the elements of the said array with spaces:")
  print(r)
  
  Q.9.write a numpy program to split the elements of a given array to multiple lines.
  import numpy as np
  x=np.array(['python exercises','python','java'],dtype=np.str)
  print("original array:")
  print(x)
  r=np.char.splitlines(x)
  print(r)
  
  Q.10. write a numoy program to make all the element of a given string to a numeric string of a 5 digits with zeroes on its left.
  import numpy as np
  x=np.array(['2','11','234','1234','12345']dtype=np.str)
  print("\Original array:")
  print(x)
  r=np.char.zfill(x,5)
  print("\nNumeric string of a five digit with zeros:")
  print(r)
